#
# Release workflow
#

name: Release
on:
  push:
    tags:
      - v*

jobs:
  release-tag:
    permissions:
      contents: write
    strategy:
      matrix:
        go-version: [ 1.18.x ]
        os: [ ubuntu-latest ]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checking out the code
        id: checkout-the-code
        uses: actions/checkout@v3

      - name: Create CLI Release
        id: create-cli-release
        uses: softprops/action-gh-release@v1
        env:
          TAG_NAME: ${{ github.GITHUB_REF }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY_NAME: ${{ github.GITHUB_REPOSITORY }}
        with:
          body: |
            CLI Package Docker Image:
            ```
            docker pull ${{ REPOSITORY_NAME }}:${{ $TAG_NAME }}
            ```
          tag_name: ${{ github.GITHUB_REF }}
          name: ${{ github.GITHUB_REF }}
          draft: false
          prerelease: false