#
# Release workflow
#

name: Release
on:
  push:
    tags:
      - v*

jobs:
  release-tag:
    permissions:
      contents: write
    strategy:
      matrix:
        go-version: [ 1.18.x ]
        os: [ ubuntu-latest ]
    runs-on: ${{ matrix.os }}
    env:
      TAG: ${{ github.GITHUB_REF }}
      NAME: ${{ github.GITHUB_REPOSITORY }}
    steps:
      - name: Checking out the code
        id: checkout-the-code
        uses: actions/checkout@v3

      - name: Create CLI Release
        id: create-cli-release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          body: |
            CLI Package Docker Image:
            ```
            docker pull $NAME:$TAG
            docker pull ${{ github.GITHUB_REPOSITORY }}:${{ github.GITHUB_REF }} 
            docker pull $github.GITHUB_REPOSITORY:$github.GITHUB_REF 
            ```
          tag_name: ${{ github.GITHUB_REF }}
          name: ${{ github.GITHUB_REF }}
          draft: false
          prerelease: false